<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.elefher.cpuhandler"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACCESS_SUPERUSER" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="19" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.elefher.cpuhandler.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.elefher.tab.Info"
            android:label="Info" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.elefher.cpuhandler.MainActivity" />
        </activity>
        <activity
            android:name="com.elefher.tab.ControlCpu"
            android:label="Control Cpu" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.elefher.cpuhandler.MainActivity" />
        </activity>
        <activity
            android:name="com.elefher.tab.Voltages"
            android:label="Voltages" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.elefher.cpuhandler.MainActivity" />
        </activity>
        <activity
            android:name="com.elefher.cpuhandler.ServicesAfterBooting"
            android:label="Services" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.elefher.cpuhandler.MainActivity" />
        </activity>

        <receiver
            android:name="ServicesAfterBooting"
            android:enabled="true"
            android:exported="true"
            android:label="Boot Notification Trickle Cpu Handler" >
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>